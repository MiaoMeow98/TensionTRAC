dataset:
  root: ../../SurgTension
  annotations: data/annotation/tension_clip_annotations.csv
  label_mode: cascade
  flatten: true
  require_feature: true
  include_all_clips: false
evaluation:
  classifier: mlp
  split: stratified
  k_fold: true
  n_splits: 5
  seed: 0
  output_csv: results/mlp_cascade_stratified_split.csv
  hidden_dim: 256
  dropout: 0.3
  lr: 0.001
  weight_decay: 0.0001
  batch_size: 64
  max_epochs: 100
  patience: 15
  val_fraction: 0.15
runs:
- name: slowfast_stratified_cascade_mlp
  feature: slowfast
- name: timesformer_stratified_cascade_mlp
  feature: timesformer
- name: video_swin_stratified_cascade_mlp
  feature: video_swin
- name: tension_trac_stratified_cascade_mlp
  feature: tension_trac
